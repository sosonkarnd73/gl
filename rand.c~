#include"rand.h"
//Returns a random number
int r_num(){
  int v;
  clock_t c;
  if(c%2==0){
    c = clock()%100;
    srand((unsigned int)c);
  }
    v = (int)roundl(rand())%10;
    //printf("%d ",v);
 return v;
}

int main(int args, char** argv){
  int a, b,x,c;
  if(args!=4){
    printf("Usage ./%s a b c",argv[0]);
    exit(0);
  }
  a = atoi(argv[1]);
  b = atoi(argv[2]);
  c = atoi(argv[3]);
  while(c>0){
    x = (r_num()+a)%b;
    printf("%d\t",x);
    c--;
  }
  printf("\n");
 return 0;
}
/*int k = 99089,v[k],i = 0, n = k;
  double sum=0;
  double mean;
  while(k){
    clock_t c;
    if(k%2 == 0)
      {
	c = clock()%100;
	srand((unsigned int)c);
      }
    v[i] = (int)roundl(rand())%10;
    //printf("%d ",v[i]);
    sum = sum + v[i];
    k--;
  }
  mean = sum/n;
  printf("Mean : %F\t",mean);
  */
