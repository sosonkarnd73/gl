#include<iostream>
#include<vector>
/* Use glew.h instead of gl.h to get all the GL prototypes declared */
//#include <GL/glew.h>
///* Using the GLUT library for the base windowing setup */
//#include <GL/freeglut.h>

using namespace std;
float inner(vector<float> v1, vector<float> v2){
  float f=0.0;
  for (auto& v :v1){
    cout<<v<<" ";
  }
}
int main(){
  vector<float> v1={10,10,20};
  v1.
  // v1.push_back(20);
  // v1.push_back(30);
  // v1.push_back(40);
  // v1.push_back(50);

  cout<<"v1 : ";
  for (auto& v :v1){
    cout<<v<<" ";
  }
  cout<<endl;
  return 0;
}
